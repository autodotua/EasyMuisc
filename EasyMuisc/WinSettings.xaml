<Window x:Class="EasyMuisc.WinSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyMuisc"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Title="设置" SizeToContent="WidthAndHeight" WindowStyle="ToolWindow">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ButtonBase}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" ContentStringFormat="{TemplateBinding ContentStringFormat}" Focusable="False" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="5,3" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Button.IsDefaulted" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="border" Value="#FFBEE6FD"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#FF3C7FB1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" TargetName="border" Value="#FF73CFFF"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#FF2C628B"/>
                            </Trigger>
                            <Trigger Property="ToggleButton.IsChecked" Value="True">
                                <Setter Property="Background" TargetName="border" Value="#FFBCDDEE"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#FF245A83"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" TargetName="border" Value="#FFF4F4F4"/>
                                <Setter Property="BorderBrush" TargetName="border" Value="#FFADB2B5"/>
                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="#FF838383"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <StackPanel Margin="4">
        <CheckBox x:Name="chkOffset" Content="通过Offset标签而不是改变全部时间来保存偏移的Lrc歌词"/>
        <TextBlock FontSize="5"/>
        <CheckBox x:Name="chkPreferMusicInfo" Content="保存歌词时优先采用歌曲信息而非原歌词信息"/>
        <TextBlock FontSize="5"/>
        <CheckBox x:Name="chkLrcAnimation" Content="歌词动画" />
        <TextBlock FontSize="5"/>
        <StackPanel Orientation="Horizontal">
            <RadioButton x:Name="chkListBoxLrc" Content="ListBox歌词         "/>
            <RadioButton x:Name="chkStackPanel" Content="StackPanel歌词"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="刷新检测速度（n/s）：   " />
            <TextBox x:Name="txtUpdateSpeed" BorderThickness="0, 0,0,1" MinWidth="20"  />
        </StackPanel>
        <TextBlock FontSize="5"/>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="动画帧率（frame/s）：   " />
            <TextBox x:Name="txtAnimationFps" BorderThickness="0, 0,0,1" MinWidth="20"  />
        </StackPanel>
        <TextBlock FontSize="5"/>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="歌词默认偏移（s）：     " />
            <TextBox x:Name="txtOffset" BorderThickness="0, 0,0,1" MinWidth="20" />
        </StackPanel>
        <TextBlock FontSize="5"/>
        <Button Content="保存" Click="ButtonClickEventHandler"/>
    </StackPanel>
</Window>
